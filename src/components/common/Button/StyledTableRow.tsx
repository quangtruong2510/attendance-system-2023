import React, { useEffect } from 'react';

interface Props {
  id: number;
  onIconClick: (id: string) => void;
}

const StyledTableRow: React.FC<Props> = (props: Props) => {
return (
  <div>Edit</div>
);
}
export default StyledTableRow;